import { defaultSystem } from "@chakra-ui/react";

// Central place to customize Chakra v3 System tokens.
// TODO: replace with a custom system (brand colors, semantic tokens, recipes).
export const system = defaultSystem;

